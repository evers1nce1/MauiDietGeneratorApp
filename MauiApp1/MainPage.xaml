<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage"
             BackgroundColor="White">

    <ScrollView>
        <VerticalStackLayout
            Padding="20"
            Spacing="25"
            VerticalOptions="Center"
            BackgroundColor="White">

            <!-- Логотип / Изображение -->
            <Image
                Source="carrot.png"
                HeightRequest="130"
                WidthRequest="130"
                Aspect="Fill" 
                HorizontalOptions="Center"
                BackgroundColor="White"/>

            <!-- Заголовок -->
            <Label
                Text="Reciper"
                FontSize="32"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                TextColor="Black" />

            <!-- Описание -->
            <Label
                Text="Приложение для составления диеты"
                FontSize="20"
                HorizontalOptions="Center"
                HorizontalTextAlignment="Center"
                TextColor="Black" />

            <!-- Кнопка "Начать" -->
            <Button
                x:Name="StartButton"
                Text="Начать"
                BackgroundColor="#6200EE" 
                TextColor="White"  
                FontSize="22"
                CornerRadius="10"
                HeightRequest="50"
                Clicked="OnStartButtonClick"
                HorizontalOptions="Fill" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
